<div class="col-md-12">
  <h2>Member Questions</h2>
  <table class="table">
    <thead>
      <tr>
        <th>Title</th>
        <th>User</th>
        <th>Status</th>
        <th>Created_at</th>
        <th colspan="2"></th>
      </tr>
    </thead>
    <tbody>
      <% @mqs.each do |mq| %> 
        <tr>
          <td> <%= link_to admin_member_question_path(mq.user) do %><%= mq.title %><% end %> </td>
          <td> <%= mq.user.id %> </td>
          <td> <%= mq.status%> </td>
          <td> <%= mq.created_at%> </td>
          <td> <% link_to("Edit", 
                           edit_admin_member_question_path(mq), 
                           :class => "btn btn-default btn-xs")%>

               <% link_to("Delete", 
                           admin_member_question_path(mq), 
                           :class => "btn btn-default btn-xs ",
                           :method => :delete,
                           data: { confirm: "Are you sure?" } )%></td>
        </tr>
      <% end %> 
    </tbody>
  </table>
</div>