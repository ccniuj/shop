$('#contact_id').val("<%= j(@contact.id.to_s) %>")
$('#name').val("<%= j(@contact.name) %>")
$('#email').val("<%= j(@contact.email) %>")
$('#cellphone').val("<%= j(@contact.cellphone) %>")
$('#address').val("<%= j(@contact.address) %>")