<h1 class="text-center">我的訂單</h1>
<div class="col-md-12"> 
  <div class="group">
  </div>
  <table class="table table-hover">
    <thead> 
      <tr>
        <th> 訂單編號 </th>
        <th> 收件人 </th>
        <th> 付款方式 </th>
        <th> 收件方式 </th>
        <th> 總金額 </th>
        <th> 狀態 </th>
      </tr>
    </thead>
    <tbody>
      <% @orders.each do |o| %>
        <tr>
        <td> <%= link_to order_path(o) do%><%= o.id %><% end %> </td>
        <td> <%= @contacts.find(o.contact_id).name %> </td>
        <td> <%= o.pay_method %> </td>
        <td> <%= o.ship_method %> </td>
        <td> <%= o.total_price %> </td>
        <td> <%= o.status %> </td>
        </tr>
      <% end %> 
    </tbody>
  </table>
</div>
